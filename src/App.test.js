import { render, screen } from '@testing-library/react';
import App from './App';

test('Renders the App', () => {
  render(<App />);
  // Left empty until we learn more about the RTL and Screen.
});
